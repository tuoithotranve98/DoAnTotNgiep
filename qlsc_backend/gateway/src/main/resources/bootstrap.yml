spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8761

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

zuul:
  ignored-services: "*"

server:
  port: 8762
